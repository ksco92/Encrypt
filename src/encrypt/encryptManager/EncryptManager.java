package encrypt.encryptManager;

import behaviors.DecryptMessageBehavior;
import behaviors.EncryptMessageBehavior;
import tools.KeyCreator;

public class EncryptManager implements EncryptMessageBehavior, KeyCreator, DecryptMessageBehavior {

}
